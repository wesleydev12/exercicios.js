const [a, b, c, d] = [3, 5, 1, 15]

const soma = a + b + c + d
const subtracao = d - b
const multiplicacao = a * d
const divisao = d/a
const modulo = a % 2 // ele pega o A e divide por dois e o resto da divisao ele da como resposta
// se a é numero par o resto da divisao é ZERO

console.log(soma, subtracao, multiplicacao, divisao, modulo)














// POSTFIX    d++
// PREFIX     ++d